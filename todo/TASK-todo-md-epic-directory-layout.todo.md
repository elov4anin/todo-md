---
type: fix
created: 2026-08-29 14:01:06 (1787994066)
due: 
started: 
completed: 
cancelled: 
value: V3
complexity: C3
priority: P1
cost_plan: 
cost_fact: 
depends_on: 
epic: 
author: Архитектор (codex)
assignee: 
branch: 
pr: 
status: todo
---

# TASK-todo-md-epic-directory-layout: Согласовать CLI со структурой каталогов эпиков

## 0. Простое описание (Human Brief)

### Проблема простыми словами (Problem)
- Руководство требует хранить эпик в собственном каталоге `EPIC-*`, а связанную задачу — рядом с ним в каталоге этого эпика внутри соответствующей зоны доски.
- `npx todo-md create` сейчас всегда создаёт плоские пути вида `todo/TASK-*.todo.md` и `todo/EPIC-*.todo.md`, даже когда передан `--epic`.
- Команды смены статуса строят целевой путь только из зоны доски и имени файла, поэтому вложенная задача после `start`, `review`, `backlog`, `done` или `cancel` теряет каталог эпика.
- CLI противоречит собственной документации, а пользователь не может полагаться на единую структуру файлов при создании и ведении эпиков.

### Варианты или путь решения (Solution Sketch)
- Ввести одну функцию вычисления канонического пути элемента по статусу, виду элемента и полю `epic`.
- Использовать её в создании, переходах и изменении поля `epic`; рекурсивный поиск оставить общей основой для чтения.
- Старые плоские файлы продолжать находить, а при первой изменяющей команде переносить в канонический путь с безопасным переписыванием ссылок.

### Ожидаемый результат (Expected Result)
- Эпики и входящие в них задачи автоматически сохраняют документированную структуру каталогов во всех состояниях доски.
- Все CLI-команды одинаково видят вложенные файлы, не ломают локальные Markdown-ссылки и не требуют ручного перемещения.
- Существующие доски с плоскими файлами продолжают читаться и могут быть постепенно нормализованы без отдельной обязательной миграции.

## 1. Концепция и Цель (Concept and Goal)

### История (User Story)
> **User Story:** Как пользователь `todo-md`, я хочу, чтобы CLI сам размещал эпики и связанные задачи в каталогах `EPIC-*` и сохранял эту структуру при смене статуса, чтобы файловая доска соответствовала руководству и оставалась предсказуемой.

### Цель по SMART (Goal)
- За одну задачу согласовать `create`, переходы, `set`, `validate`, экспорт и dashboard с единым контрактом путей эпиков; подтвердить сценарии интеграционными тестами и успешным `npm run check`.

## 2. Контекст и Границы (Context and Scope)

- **Где делаем:** `src/board.ts`, `src/parser.ts`, `src/validator.ts`, `src/cli.ts`, тестовые helpers и интеграционные тесты в `tests/`; синхронно обновить `dist/` и пользовательскую документацию CLI.
- **Текущее поведение:** `create()` формирует `todo/<zone>/<ID>.todo.md`; `transition()` переносит в `todo/<zone>/<basename>`; `set epic=...` меняет только front matter. Рекурсивные `findTodoFiles()` и `findFileById()` уже находят вложенные файлы.
- **Канонический контракт:**
  - активный эпик: `todo/EPIC-x/EPIC-x.todo.md`;
  - активная задача эпика: `todo/EPIC-x/TASK-y.todo.md`;
  - backlog: `todo/backlog/EPIC-x/<ID>.todo.md`;
  - done: `todo/done/EPIC-x/<ID>.todo.md`;
  - cancelled: `todo/cancelled/EPIC-x/<ID>.todo.md`;
  - самостоятельная задача остаётся плоской: `todo/<zone>/TASK-y.todo.md`.
- **Правило группировки:** эпик использует каталог собственного ID; задача использует каталог из `epic`; статус каждого файла независим, поэтому переход эпика не переносит его дочерние задачи целиком.
- **Обратная совместимость:** плоские эпики и плоские задачи с заполненным `epic` должны оставаться доступными для чтения и операций; изменяющая команда нормализует только затронутый файл.
- **Границы (Out of Scope):** массовая миграция существующих досок, новая команда миграции, изменение YAML-схемы, новый статус, перемещение всех элементов эпика одной командой, рефакторинг dashboard вне поддержки вложенных путей.

## 3. Требования, MoSCoW (Requirements)
### 🔴 Обязательно (Must Have)
- [ ] Реализовать общий безопасный вычислитель канонического пути; значения ID и `epic` с разделителями, `..` или иным выходом за `todo/` отклонять до файловой операции.
- [ ] `create EPIC-x` создавать `todo/EPIC-x/EPIC-x.todo.md`, а при `--status=backlog` — `todo/backlog/EPIC-x/EPIC-x.todo.md`.
- [ ] `create TASK-y --epic=EPIC-x` проверять существование и вид эпика и создавать `todo/EPIC-x/TASK-y.todo.md`; для `--status=backlog` использовать `todo/backlog/EPIC-x/TASK-y.todo.md`.
- [ ] `create TASK-y` без `--epic` сохранить текущий плоский путь и поведение.
- [ ] `start`, `review`, `backlog`, `done`, `cancel` и `set status=...` перемещать файл между зонами, сохраняя каталог эпика; операция над самим эпиком перемещает только файл эпика.
- [ ] Переход активного статуса в другой активный статус также нормализует старый плоский файл в каталог эпика, даже если зона доски не меняется.
- [ ] `set TASK-y epic=EPIC-x`, смена `epic` и очистка `epic=` атомарно перемещают задачу в правильный каталог текущей зоны; ошибочная ссылка или коллизия полностью откатывает front matter, путь и ссылки.
- [ ] При любом перемещении корректно переписывать исходящие и входящие относительные Markdown-ссылки с учётом дополнительного уровня каталога; HTTP(S), anchors и изображения не изменять.
- [ ] `validate` проверять согласованность вложенного каталога с ID эпика и полем `epic`; ошибочный чужой каталог считать ошибкой. Для старого плоского корректного файла выдавать совместимое предупреждение, а не делать доску нечитаемой.
- [ ] `findFileById`, точечный и полный `validate`, `export-jsonl` и `dashboard` продолжать рекурсивно обрабатывать вложенные задачи; экспортируемое поле `file` должно содержать фактический вложенный путь, а `folder` — правильную зону статуса.
- [ ] Обновить `--help`, README и копируемую документацию там, где описано размещение `create`, переходов и `set epic`; примеры должны совпадать с каноническим контрактом.
- [ ] После изменения `src/` выполнить `npm run compile` и включить обновлённый `dist/` в тот же коммит.

### 🟡 Желательно (Should Have)
- [ ] Вынести построение зоны и каталога группировки в небольшие функции без дублирования строковых путей между `create`, `transition`, `set` и validator.
- [ ] Тестовые helpers должны возвращать реальный созданный путь из ожидаемого канонического расположения и позволять создавать связанные task/epic fixtures.

### 🟢 Опционально (Could Have)
- [ ] Удалять опустевший каталог `EPIC-*` после успешного перемещения, только если это не усложняет атомарный rollback.

### ⚫ Won't Have (Не будем делать)
- Не добавлять внешние зависимости.
- Не перемещать автоматически соседние задачи при переходе эпика.
- Не менять публичный формат front matter и JSONL-записей.
- Не следовать символическим ссылкам и не ослаблять защиту путей.
- Не превращать предупреждение о legacy-layout в обязательную массовую миграцию в рамках этой задачи.

## 4. План реализации (Implementation Plan)
1. [ ] Зафиксировать таблицу канонических путей и добавить красные тесты для `create` task/epic в active и backlog.
2. [ ] Реализовать безопасное вычисление пути и подключить его к `create`.
3. [ ] Покрыть и исправить все переходы, включая нормализацию legacy-layout и переписывание ссылок.
4. [ ] Сделать изменение `epic` структурной атомарной операцией в `setFields`.
5. [ ] Добавить структурные проверки validator и регрессии для рекурсивного поиска, точечной валидации, JSONL и dashboard.
6. [ ] Обновить справку и документацию, скомпилировать `dist`, выполнить полный check.

## 5. Критерии приёмки (Definition of Done)
- [ ] Интеграционные тесты подтверждают все шесть примеров канонического контракта из раздела 2.
- [ ] Создание с неизвестным `--epic`, ID задачи вместо эпика и небезопасным значением завершается ошибкой без оставшихся файлов или каталогов.
- [ ] Последовательность `create --epic` → `backlog` → `start` → `review` → `done` сохраняет подкаталог `EPIC-x` в каждой зоне и валидный front matter.
- [ ] `set epic=EPIC-x`, смена эпика и `set epic=` перемещают файл в ожидаемый путь; rollback проверен негативным тестом.
- [ ] Относительные ссылки в обе стороны остаются разрешимыми после перемещения вложенного файла; транзакционный rollback не оставляет частичных изменений.
- [ ] Legacy-плоские файлы находятся всеми командами, получают предупреждение validator и нормализуются при изменяющей операции.
- [ ] `export-jsonl` и `dashboard` включают вложенные файлы без дублей и с корректными `file`, `folder`, `status`, `epic`.
- [ ] Справка CLI и документация не содержат примеров, противоречащих новой структуре.
- [ ] `npm run check` проходит; `src/` и `dist/` синхронизированы.

## 6. Самопроверка (Verification)
```bash
npm run check
npm_config_cache=/tmp/todo-md-npx-cache npx --no-install todo-md validate
npm_config_cache=/tmp/todo-md-npx-cache npx --no-install todo-md validate todo/TASK-todo-md-epic-directory-layout.todo.md
```

## 7. Риски и зависимости (Risks and Dependencies)
- **Риск обратной совместимости:** немедленная ошибка для старого плоского layout сломает CI потребителей; поэтому чтение сохраняется, а нормализация выполняется постепенно.
- **Риск потери ссылок:** дополнительный уровень вложенности меняет относительные пути; сценарии inbound/outbound и rollback обязательны.
- **Риск выхода за рабочий каталог:** путь строится из front matter; до `resolve()` нужно проверить ID и гарантировать нахождение результата внутри `todo/`.
- **Зависимости:** отсутствуют; рекурсивный обход и транзакция уже реализованы в `src/parser.ts` и `src/transaction.ts`.

## 8. Источники (Sources)

- [Руководство по написанию задач](../docs/todo-md/AGENTS_TASK_WRITING_GUIDE.md) — правила размещения задач и эпиков.
- [Шаблон задачи](../docs/todo-md/templates/task.md).
- `src/board.ts` — текущие `create`, `transition`, `setFields` и переписывание ссылок.
- `src/parser.ts` — рекурсивный поиск и определение зоны.
- `src/validator.ts` — проверка статуса по каталогу без проверки каталога эпика.
- `src/cli.ts`, `src/export.ts` — команды чтения и пользовательская справка.

## 9. Комментарии (Comments)

- `init` уже создаёт базовые зоны; каталоги `EPIC-*` должны создаваться лениво файловой транзакцией.
- Текущий рекурсивный обход означает, что `validate`, экспорт и dashboard в основном готовы к вложенности; для них нужны контрактные регрессии, а не отдельная реализация обхода.
- Канонический путь определяется метаданными конкретного файла, а не физическим положением файла эпика: эпик и его задачи могут находиться в разных статусах.

## История изменений (Change History)
| Дата | Автор (роль) | Изменение |
| :--- | :--- | :--- |
| 2026-08-29 14:01:06 (1787994066) | Архитектор (codex) | Создание задачи |
