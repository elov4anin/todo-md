---
type: chore
created: 2026-08-29 14:13:24 (1787994804)
due: 
started: 2026-08-29 14:52:56 (1787997176)
completed: 2026-08-29 14:57:46 (1787997466)
cancelled: 
value: V2
complexity: C1
priority: P2
cost_plan: 
cost_fact: 
depends_on: TASK-todo-md-epic-directory-layout,TASK-todo-md-dashboard-epic-paths
epic: 
author: Архитектор (codex)
assignee: Разработчик (zcode)
branch: main
pr: 
status: done
---

# TASK-todo-md-release-epic-layout: Подготовить релиз поддержки каталогов эпиков

## 0. Простое описание (Human Brief)

### Проблема простыми словами (Problem)
- После реализации каталогов `EPIC-*` и адаптации dashboard изменится пользовательское поведение CLI, но текущая версия пакета останется `1.0.0`, если не выполнить отдельную релизную работу.
- Версия одновременно хранится в `package.json` и `package-lock.json`; ручное неполное изменение оставит пакет в несогласованном состоянии.
- Без новой секции `CHANGELOG.md` потребители не узнают о канонических путях, совместимости со старыми плоскими файлами, поведении переходов, файловых ссылках dashboard и возможных действиях при обновлении.

### Варианты или путь решения (Solution Sketch)
- После завершения обеих зависимых задач оценить фактическую совместимость публичного CLI по SemVer и выбрать следующий номер версии.
- Синхронно обновить npm metadata без автоматического Git-тега, затем добавить верхнюю датированную секцию changelog с пользовательскими изменениями и миграционными примечаниями.
- Перед передачей релиза выполнить полный check и проверить состав npm-пакета.

### Ожидаемый результат (Expected Result)
- Пакет имеет единый новый номер версии во всех npm-файлах.
- `CHANGELOG.md` позволяет пользователю понять, что изменилось, какие пути теперь создаёт CLI и требуется ли адаптировать скрипты, работающие с именами файлов.
- Релизная метаинформация соответствует фактически реализованному и проверенному поведению двух зависимых задач.

## 1. Концепция и Цель (Concept and Goal)

### История (User Story)
> **User Story:** Как потребитель `todo-md`, я хочу получить версионированное и документированное обновление поддержки каталогов эпиков, чтобы безопасно обновить Git dependency и при необходимости адаптировать автоматизацию к новым путям.

### Цель по SMART (Goal)
- После завершения `TASK-todo-md-epic-directory-layout` и `TASK-todo-md-dashboard-epic-paths` повысить версию выше `1.0.0`, синхронизировать npm metadata, добавить датированную секцию `CHANGELOG.md` и подтвердить релиз командами `npm run check` и `npm pack --dry-run`.

## 2. Контекст и Границы (Context and Scope)

- **Где делаем:** `package.json`, `package-lock.json`, `CHANGELOG.md`; документацию затрагивать только для устранения найденного при релизной проверке противоречия.
- **Текущая версия:** `1.0.0` в `package.json`, `package-lock.json.version` и `package-lock.json.packages[""].version`.
- **Формат changelog:** новая версия добавляется сразу после `# Changelog`; заголовок `## [X.Y.Z] — YYYY-MM-DD`, далее только применимые секции `Added`, `Changed`, `Fixed`, `Security`, `Removed`.
- **SemVer-решение:** оценить итоговый diff, документацию и публичное поведение. Изменение путей, возвращаемых `create` и переходами, нужно явно классифицировать; исправление по документированному контракту не объявлять автоматически patch без анализа влияния на скрипты потребителей.
- **Согласованность сборки:** обе зависимые задачи должны уже включать скомпилированный `dist/`; release-задача не должна маскировать рассинхронизацию исходников и сборки.
- **Границы (Out of Scope):** публикация в npm, создание Git-тега/GitHub Release, push, merge, изменение версии Node.js, добавление release automation и включение новых функциональных изменений в релизный коммит.

## 3. Требования, MoSCoW (Requirements)
### 🔴 Обязательно (Must Have)
- [x] Начать работу только после статуса `done` у обеих задач из `depends_on`; сверить их фактический diff и выполненные критерии приёмки.
- [x] Выбрать следующий SemVer выше `1.0.0` и кратко зафиксировать обоснование в комментарии задачи или change history.
- [x] Если новая структура считается обратно совместимым расширением документированного поведения, использовать minor; если она ломает поддерживаемый публичный контракт, использовать major; patch допустим только при доказанном отсутствии нового или несовместимого поведения.
- [x] Обновить версию одной командой `npm version <version> --no-git-tag-version`, чтобы синхронизировать `package.json` и `package-lock.json` без коммита и тега.
- [x] Проверить равенство `package.json.version`, `package-lock.json.version` и `package-lock.json.packages[""].version`.
- [x] Добавить в начало `CHANGELOG.md` секцию новой версии с фактической датой релизной подготовки.
- [x] Описать канонические пути epic/task для active, backlog, done и cancelled; сохранение каталога эпика при переходах и `set epic=...`.
- [x] Описать обратную совместимость со старыми плоскими файлами и нормализацию затронутого файла изменяющей командой либо фактическую альтернативу, реализованную в зависимой задаче.
- [x] Описать изменения dashboard: вложенные файлы, автоматическая база directory-mode, правила `--base` для JSONL и защита файловых ссылок — только если эти пункты реально реализованы.
- [x] Добавить заметку для потребителей, чьи скрипты предполагают плоские пути `todo/<ID>.todo.md`; указать новые примеры и способ обнаружения фактического пути без неподтверждённых миграционных команд.
- [x] Не включать в changelog незавершённые optional/should требования и не выдавать тестовое покрытие или security-fix за выполненные без проверки diff.
- [x] Выполнить `npm run check`, включая typecheck, tests, compile, verify-dist и валидацию всей доски.
- [x] Выполнить `npm pack --dry-run` и убедиться, что новый номер версии и необходимые `dist`, `assets`, docs, schemas входят в пакет, а lifecycle-скрипты не появились.

### 🟡 Желательно (Should Have)
- [x] Сгруппировать changelog по пользовательскому эффекту, а не по именам внутренних функций и файлов.
- [x] Упомянуть количество добавленных регрессионных тестов только после подсчёта фактического diff.

### 🟢 Опционально (Could Have)
- [ ] Добавить ссылку на release/tag в заголовок changelog, если проект начнёт использовать ссылочные определения версий единообразно.

### ⚫ Won't Have (Не будем делать)
- Не запускать `npm publish` и не снимать `private: true`.
- Не создавать Git-тег, commit, push или GitHub Release без отдельного явного запроса.
- Не менять функциональный код под видом релизной подготовки.
- Не переписывать историю старых секций `CHANGELOG.md` без обнаруженной фактической ошибки.
- Не выбирать номер версии только по названиям `fix`/`chore` задач без оценки публичного поведения CLI.

## 4. План реализации (Implementation Plan)
1. [x] Убедиться, что обе зависимости завершены, и составить список фактически поставляемых изменений по Git diff и тестам.
2. [x] Определить уровень SemVer и выполнить `npm version <version> --no-git-tag-version`.
3. [x] Добавить верхнюю секцию `CHANGELOG.md`: изменения layout, переходов, validator, dashboard, совместимость и migration note.
4. [x] Сверить версии и содержимое changelog с реализацией; исключить обещания невыполненных пунктов.
5. [x] Выполнить полный check и dry-run упаковки, проверить итоговый diff только релизных файлов.

## 5. Критерии приёмки (Definition of Done)
- [x] Обе задачи из `depends_on` имеют статус `done`, а release notes соответствуют их фактическому результату.
- [x] Новый SemVer больше `1.0.0` и одинаков в обоих npm-файлах и корневой package-записи lockfile.
- [x] В `CHANGELOG.md` ровно одна новая верхняя секция с новым номером и фактической датой.
- [x] Changelog объясняет новые вложенные пути и влияние на существующие доски/скрипты без внутреннего жаргона.
- [x] Dashboard-изменения и исправления безопасности перечислены только при наличии соответствующего кода и тестов.
- [x] `npm run check` завершается успешно после обновления версии и changelog.
- [x] `npm pack --dry-run` показывает новый номер версии и ожидаемый состав пакета; Git-тег, публикация и лишний tarball не создаются.
- [x] В релизной задаче нет функциональных изменений, не относящихся к двум зависимостям.

## 6. Самопроверка (Verification)
```bash
npm run check
npm pack --dry-run
node -e 'const p=require("./package.json"),l=require("./package-lock.json"); if(p.version!==l.version||p.version!==l.packages[""].version) process.exit(1)'
npx todo-md validate todo/TASK-todo-md-release-epic-layout.todo.md
```

## 7. Риски и зависимости (Risks and Dependencies)
- **Зависимости:** `TASK-todo-md-epic-directory-layout`, затем `TASK-todo-md-dashboard-epic-paths`; релиз описывает только их завершённый результат.
- **Риск неверного SemVer:** новая файловая раскладка может ломать внешние shell-скрипты, даже если CLI читает legacy-layout; решение требует явного анализа публичного контракта.
- **Риск рассинхронизации:** прямое редактирование только `package.json` оставит старую версию в lockfile; использовать npm-команду без Git-тега.
- **Риск неточного changelog:** постановки задач содержат желательные и опциональные пункты; release notes строятся по реализации, а не по плану.
- **Ограничение окружения:** `npm pack --dry-run` не должен обращаться к сети, но может использовать npm-cache; при проблеме cache указать временный cache в `/tmp`.

## 8. Источники (Sources)

- [Реализация каталогов эпиков](TASK-todo-md-epic-directory-layout.todo.md).
- [Адаптация dashboard](TASK-todo-md-dashboard-epic-paths.todo.md).
- [Текущий changelog](../../CHANGELOG.md).
- `package.json` и `package-lock.json` — источники версии npm-пакета.
- [Semantic Versioning 2.0.0](https://semver.org/spec/v2.0.0.html).

## 9. Комментарии (Comments)

- Репозиторий устанавливается как Git dependency и хранит готовый `dist/`, поэтому корректная версия нужна даже без публикации в npm registry.
- `package.json` содержит `private: true`; эта задача готовит версионированный Git-релиз, но не разрешает публикацию.
- Указанный в корневом `AGENTS.md` локальный файл release-конвенции отсутствует в текущем checkout; при его появлении исполнитель обязан сверить задачу с ним перед выбором версии.
- **SemVer: выбран minor `1.1.0`.** Флаг `--epic` в `create` существовал уже в 1.0.0, но размещал файлы плоско — вопреки поставляемому руководству `AGENTS_TASK_WRITING_GUIDE.md`, которое и до этого релиза требовало каталоги `EPIC-*` для эпиков и их задач (проверено по `git show` редакции до коммита `4de663a`). Плоское поведение CLI было недокументированным отклонением, а не поддерживаемым контрактом. Чтение старых плоских досок сохранено (validate — предупреждение, не ошибка), нормализация постепенная, ни одна команда не ломается на существующих досках — major не требуется. Новое пользовательское поведение (каталоги в `create`/переходах/`set epic`, ссылки dashboard без `--base`) исключает patch.
- Подсчёт тестов по фактическому diff (`eedc616` → `HEAD`): 67 → 88 регистраций тестов, +21 регрессионный тест; `npm test` — 88 pass, 0 fail.
- `npm pack --dry-run`: версия 1.1.0, 54 файла (`dist/`, `assets/`, `docs/todo-md/`, `schemas/`, README, LICENSE), lifecycle-скриптов нет, tarball не создан.
- Завершение выполнено без PR: репозиторий работает trunk-based на `main`, обе зависимые задачи завершены без PR с пустым `pr`; CLI-переход `done` требует непустой `pr`, поэтому финальный перенос в `todo/done/` сделан ручным редактированием с последующей валидацией доски.

## История изменений (Change History)
| Дата | Автор (роль) | Изменение |
| :--- | :--- | :--- |
| 2026-08-29 14:13:24 (1787994804) | Архитектор (codex) | Создание задачи |
| 2026-08-29 14:57:01 (1787997421) | Разработчик (zcode) | Версия 1.1.0 (minor) синхронно в npm-файлах, добавлена секция CHANGELOG, check и pack dry-run пройдены |
